window.cipher = {
  encode: (offset, string) => {
    /* Acá va tu código */
    encode(offset, string)
  },
  decode: (offset, string) => {
    /* Acá va tu código */
    decode(offset, string)
  }
}